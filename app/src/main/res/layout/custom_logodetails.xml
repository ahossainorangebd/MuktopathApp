<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ActionBarWrapper"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:background="#7a19aa"
    >

    <ImageView
        android:id="@+id/rtvheadlogo"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:src="@drawable/muktopath_logo"
        android:padding="5dp"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        />

    <EditText
        android:id="@+id/searchViewId"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:focusableInTouchMode="true"
        android:descendantFocusability="beforeDescendants"
        android:hint="Search"
        android:singleLine="true"
        android:visibility="gone"
        />

    <!--<android.support.v7.widget.SearchView
        android:id="@+id/searchViewId"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        >
        <requestFocus/>

    </android.support.v7.widget.SearchView>-->

    <!--<ImageView
        android:id="@+id/searchBtnId"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/search_for_custom"
        android:layout_alignParentRight="true"
        android:padding="10dp"
        android:layout_marginRight="6dp"
        android:layout_gravity="right"
        />-->

    <ImageView
        android:id="@+id/searchCrossBtnId"
        android:layout_marginTop="12dp"
        android:layout_marginRight="18dp"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/search_cross_btn"
        android:layout_alignParentRight="true"
        android:gravity="right"
        android:padding="3dp"
        android:visibility="gone"
        />

    <!--...-->
</RelativeLayout>